{
  "version": 2,
  "builds": [
    {
      "src": "server.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["config.env"],
        "maxDuration": 30
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/server.ts" }]
}
