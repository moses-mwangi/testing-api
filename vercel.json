{
  "version": 2,
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["config.env"]
      }
    }
  ],

  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
